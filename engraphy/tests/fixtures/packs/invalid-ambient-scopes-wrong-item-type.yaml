# design/07 Pack file schema — rule under test: ambient_scopes.items type: string
# Fixture for pack validate; valid=False.
pack: fixture
version: 1
node_types:
  widget:
    description: A minimal node type for schema-fixture testing.
    attrs:
      required:
        status:
          enum:
          - open
          - closed
      optional:
        note:
          type: string
      closed: true
edge_types:
  relates_to:
    description: Generic association.
    bidirectional: true
edge_rules:
- type: relates_to
  src: widget
  dst: widget
ambient_scopes:
- 1
- 2
briefing:
  sections:
  - name: recent
    type: widget
    recent: 7d
  footer:
    inbox_pending_count: true
tool_aliases:
  log_widget:
    binds: write
    preset:
      type: widget
tool_descriptions:
  write: Custom description.
